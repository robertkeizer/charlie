Command line interface written in javascript using nodejs.

Currently supports multiple commands per line ( semicolon ). As well as piping.

Example:
	
	node cli.js
	cli>ls / | grep tmp
	tmp

Available commands are: 
	exit
	ls
	help
	printenv
	setenv
	pwd
	unsetenv
	grep
